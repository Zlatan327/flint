{
  "buildCommand": "npm --prefix web run build",
  "installCommand": "npm --prefix web install --legacy-peer-deps",
  "outputDirectory": "web/dist/public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
